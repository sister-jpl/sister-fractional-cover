# DO NOT DELETE
# THIS CONFIG IS AUTO-GENERATED BY ADE UI
algorithm_name: sister-fractional-cover
algorithm_version: 1.3.0
repository_url: https://gitlab.com/geospec/sister-fractional-cover.git
docker_container_url: mas.dit.maap-project.org/root/maap-workspaces/base_images/vanilla:dit

# fill out these fields
# explain what this algorithm does
algorithm_description: The SISTER wrapper for SpectralUnmixing, a general, fast, and flexible spectral unmixing package.
build_command: sister-fractional-cover/install.sh
# path to the wrapper script for running the algorithm
run_command: sister-fractional-cover/run.sh
# set a storage value in GB or MB or KB, e.g. "100GB", "20MB", "10KB"
disk_space: 50GB
inputs:
# remove below this line if no inputs
# rename and set algorithm input names accordingly
  file:
    - name: reflectance_dataset
      required: True
  config:
    - name: n_cores
      default: "1"
    - name: refl_scale
      default: "1.0"
    - name: crid
      default: "000"
    - name: experimental
      default: "True"